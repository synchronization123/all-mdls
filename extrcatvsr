<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Extract Version Codes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-5">
    <h3 class="mb-4">Extract Version Codes (V11/V12/R11/R12)</h3>

    <div class="mb-3">
      <label for="inputText" class="form-label">Paste your paragraph below:</label>
      <textarea class="form-control" id="inputText" rows="6" placeholder="Paste text here..."></textarea>
    </div>

    <button class="btn btn-primary mb-3" onclick="extractData()">Extract</button>

    <div class="mb-3">
      <label for="outputText" class="form-label">Extracted Data (comma separated):</label>
      <textarea class="form-control" id="outputText" rows="4" readonly></textarea>
    </div>
  </div>

  <script>
    function extractData() {
      const text = document.getElementById('inputText').value;
      const regex = /\b(V11|V12|R11|R12)[._\d]*/gi;
      const matches = text.match(regex);
      const result = matches ? matches.join(", ") : "";
      document.getElementById('outputText').value = result;
    }
  </script>

</body>
</html>